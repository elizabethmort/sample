function distance(p1, p2) {
    var dx = p1.x - p2.x;
    var dy = p1.y - p2.y;
    return Math.sqrt(dx * dx + dy * dy);
}

/*
 This function should return true if the given "word" starts with "beginning"

 e.g.
 startsWith("Hello", "Hell") -> true
 startsWith("This is a test", "This") -> true
 startsWith("My name is Mudd", "Hi") -> false
 */
function startsWith(word, beginning) {
    // HINT: remember that strings are objects with methods like substr()...e.g. word.substr(3,4)
    var lengthOfBeginning = ?;
    var startOfWord = ?;
    if (?)
        return true;
    else
        return false;
}

